// Android 端 pinyin 桩:原样返回输入,不做拼音转换。
// 完整 pinyin.uts 是转译后的 JS,无法在 UTS/Kotlin 下编译;
// agent-union-record 在 APP 端暂不提供 A-Z 拼音排序/字母索引(其它平台保留)。
export function pinyin(text: string, options?: any): string {
  return text;
}
