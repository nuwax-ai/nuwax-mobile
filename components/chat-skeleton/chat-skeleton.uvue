<template>
	<!-- 离线容器加载骨架：灰色块聊天形状（导航 + 左右气泡 + 输入条），
	     配色与 H5 侧 index.html 启动骨架一致，揭开前后视觉连续。 -->
	<view class="chat-skeleton" :style="{ paddingTop: `${getStatusBarHeight()}px` }">
		<view class="chat-skeleton__nav">
			<view class="chat-skeleton__dot" />
			<view class="chat-skeleton__nav-title" />
			<view class="chat-skeleton__dot" />
		</view>
		<view class="chat-skeleton__body">
			<view class="chat-skeleton__row">
				<view class="chat-skeleton__avatar" />
				<view class="chat-skeleton__bubble chat-skeleton__bubble--l1" />
			</view>
			<view class="chat-skeleton__row chat-skeleton__row--right">
				<view class="chat-skeleton__bubble chat-skeleton__bubble--r1" />
				<view class="chat-skeleton__avatar" />
			</view>
			<view class="chat-skeleton__row">
				<view class="chat-skeleton__avatar" />
				<view class="chat-skeleton__bubble chat-skeleton__bubble--l2" />
			</view>
			<view class="chat-skeleton__row chat-skeleton__row--right">
				<view class="chat-skeleton__bubble chat-skeleton__bubble--r2" />
				<view class="chat-skeleton__avatar" />
			</view>
		</view>
		<view class="chat-skeleton__input">
			<view class="chat-skeleton__input-bar" />
			<view class="chat-skeleton__dot" />
		</view>
	</view>
</template>
<script setup lang="uts">
import { getStatusBarHeight } from '@/utils/system.uts'
</script>
<style>
.chat-skeleton { width: 100%; height: 100%; background-color: #f5f6f7; }
.chat-skeleton__nav { height: 44px; flex-direction: row; align-items: center; justify-content: space-between; padding: 0 16px; border-bottom-width: 1px; border-bottom-color: #eef0f3; }
.chat-skeleton__nav-title { width: 96px; height: 16px; border-radius: 4px; background-color: #e3e6ea; }
.chat-skeleton__body { flex: 1; padding: 16px 16px 8px 16px; }
.chat-skeleton__row { flex-direction: row; align-items: flex-end; margin-bottom: 16px; }
.chat-skeleton__row--right { justify-content: flex-end; }
.chat-skeleton__avatar { width: 34px; height: 34px; border-radius: 17px; background-color: #e3e6ea; }
.chat-skeleton__dot { width: 28px; height: 28px; border-radius: 14px; background-color: #e3e6ea; }
.chat-skeleton__bubble { height: 40px; border-radius: 10px; background-color: #e3e6ea; margin-left: 8px; margin-right: 8px; }
.chat-skeleton__bubble--l1 { width: 58%; }
.chat-skeleton__bubble--l2 { width: 38%; height: 32px; }
.chat-skeleton__bubble--r1 { width: 46%; height: 36px; background-color: #d9dde2; }
.chat-skeleton__bubble--r2 { width: 28%; height: 28px; background-color: #d9dde2; }
.chat-skeleton__input { height: 62px; flex-direction: row; align-items: center; padding: 0 16px 20px 16px; }
.chat-skeleton__input-bar { flex: 1; height: 36px; border-radius: 18px; background-color: #e3e6ea; margin-right: 10px; }
</style>
