# Tool Call

`tool-call-card.uvue` 与 `tool-call-group.uvue` 用于 `UniAiXMsgRender` 渲染
`markdown-custom-process` 工具调用标签，不依赖 `mp-html`。

示例页面路由：`/subpackages/pages/chat-conversation-component/chat-conversation-component`

H5 访问地址：`/m/#/subpackages/pages/chat-conversation-component/chat-conversation-component`
