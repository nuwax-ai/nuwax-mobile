/**
 * 节流工具（UTS 兼容：仅保留 chat 使用的 throttle）
 */
export function throttle(func: () => void, delay: number): () => void {
  let lastTime = 0;
  return function () {
    const now = Date.now();
    if (now - lastTime >= delay) {
      lastTime = now;
      func();
    }
  };
}
