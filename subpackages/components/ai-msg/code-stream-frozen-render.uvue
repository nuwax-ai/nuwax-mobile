<template>
  <template v-for="segment in segments" :key="segment.key">
    <text
      v-if="segment.kind == CODE_STREAM_SEGMENT_PROSE"
      class="code-stream-prose"
    >{{ segment.text }}</text>
    <view v-else class="code-stream-code">
      <view class="code-stream-code-header">
        <text class="code-stream-code-language">{{ segment.language }}</text>
      </view>
      <text class="code-stream-code-text">{{ segment.text }}</text>
    </view>
  </template>
</template>

<script lang="uts" setup>
  import {
    CodeStreamSegment,
    CODE_STREAM_SEGMENT_PROSE,
  } from "@/subpackages/components/ai-msg/codeStreamDisplay.uts";

  withDefaults(
    defineProps<{
      segments?: CodeStreamSegment[];
    }>(),
    {
      segments: (): CodeStreamSegment[] => [] as CodeStreamSegment[],
    },
  );

</script>

<style lang="scss">
  .code-stream-prose {
    width: 100%;
    color: #404040;
    font-size: 32rpx;
    line-height: 48rpx;
    white-space: pre-wrap;
  }

  .code-stream-code {
    width: 100%;
    margin-bottom: 5px;
    overflow: hidden;
    background-color: var(--bg-secondary);
    border-radius: 6px;
    border: 1px solid var(--border-color);
  }

  .code-stream-code-header {
    width: 100%;
    padding: 4px 10px;
    background-color: var(--bg-tertiary);
  }

  .code-stream-code-language {
    color: var(--text-secondary);
    font-size: 12px;
  }

  .code-stream-code-text {
    width: 100%;
    padding: 8px 10px;
    color: var(--text-primary);
    font-family: monospace;
    font-size: 14px;
    line-height: 22px;
    white-space: pre;
  }
</style>
