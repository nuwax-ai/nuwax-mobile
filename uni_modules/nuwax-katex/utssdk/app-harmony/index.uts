/**
 * nuwax-katex 鸿蒙占位实现（纯 uts，无原生代码）。
 * App 端公式渲染由组件内 <web-view> 承载 static/index.html（KaTeX 实时渲染，非截图），
 * 此接口仅为让 `import { renderLatex } from '@/uni_modules/nuwax-katex'` 在 App 平台可解析。
 */
import type { KatexRenderOptions, KatexRenderRes } from '../interface.uts'
export * from '../interface.uts'

export function renderLatex(tex: string, options: KatexRenderOptions): KatexRenderRes {
	const res: KatexRenderRes = { html: '', errMsg: '' }
	return res
}
