/**
 * Mermaid 图表渲染总开关（mermaid 与公式渲染相互独立；公式走 nuwax-uni-math 原生插件，不在此处）。
 *
 * - false（默认）：mermaid 代码块不走 proxy-web 截图，直接当代码块显示（默认代码 tab）。
 * - true：恢复原 proxy-web（mermaid.js）出图链路。
 *
 * 关闭背景：list-view 回收模式下，mermaid 靠隐藏 proxy WebView 异步截图、结果绑到会被
 * 回收的 <image> 节点 →「回收即崩」（见 docs/handoff-chat-webview-leak-deadlock.md）。
 * mermaid→代码显示可拔掉该崩溃点。详见 docs/perf-list-view-migration.md。
 *
 * 注：mathjax/katex 双轨公式方案（mathRenderConfig / resolveMathAction 等）已被
 * nuwax-uni-math 原生渲染取代并从本文件移除；本文件仅保留 mermaid 开关，供
 * appMarkdownFallback / parseMarkdown / uni-ai-msg-code 共用。
 */
// 虚拟列表已回到 scroll-view + 聚合 spacer，不再使用 list-view 节点回收。
// 闭合 Mermaid 块通过 fallback 冻结、缓存并异步出图，允许流式结构化展示。
export const MERMAID_RENDER_ENABLED: boolean = true
