/**
 * Mermaid 图表渲染总开关（mermaid 与公式渲染相互独立；公式走 nuwax-uni-math 原生插件，不在此处）。
 *
 * - false：mermaid 代码块不走 proxy-web 截图，直接当代码块显示（默认代码 tab）。
 * - true（默认）：走 proxy-web（mermaid.js）出图链路（已加成功缓存命中直填 + 在途扇出 +
 *   失败 2s 冷却去重，见 appMarkdownFallback.renderMermaidToken，2026-08-07 真机验证通过）。
 *
 * 历史背景：曾因 list-view / virtual-scroll-x 回收模式（mermaid 靠隐藏 proxy WebView 异步截图、
 * 结果绑到会被回收的 <image> 节点 →「回收即崩」）而关闭。会话列表已回退为原生 scroll-view 全量
 * 渲染（不回收销毁 UniAiXMsg），该崩溃点不复存在，故恢复出图。
 *
 * 注：mathjax/katex 双轨公式方案（mathRenderConfig / resolveMathAction 等）已被
 * nuwax-uni-math 原生渲染取代并从本文件移除；本文件仅保留 mermaid 开关，供
 * appMarkdownFallback / parseMarkdown / uni-ai-msg-code 共用。
 */
// vapor: createWebviewContext 返回的 ctx 无 evalJS 方法（TypeError + 会话卡顿），
// mermaid 暂关预览、只展示源码（公式走 ratex/native 原生，不受影响）。
// 未来 vapor 适配 proxy web evalJS（UniWebViewElement.evalJS）后改回 true 开启出图。
export const MERMAID_RENDER_ENABLED: boolean = false
