<template>
  <template v-for="(chunk, index) in chunks" :key="'plain-stream-chunk-' + index">
    <text class="plain-stream-frozen-text" :style="textStyle">{{ chunk }}</text>
  </template>
</template>

<script lang="uts" setup>
  withDefaults(
    defineProps<{
      chunks?: string[];
      textStyle?: string;
    }>(),
    {
      chunks: (): string[] => [] as string[],
      textStyle: "",
    },
  );
</script>

<style lang="scss">
  .plain-stream-frozen-text {
    width: 100%;
    color: #404040;
    font-size: 32rpx;
    line-height: 48rpx;
    white-space: pre-wrap;
  }
</style>
