<template>
  <view class="related-conversation">
    <view class="header">
      <text class="title">相关会话</text>
      <view class="view-all" @click="handleViewAll">
        <text class="view-all-text">查看全部</text>
        <uni-icons class="iconfont icon-a-Chevronright" size="32rpx" color="#898a8e" />
      </view>
    </view>
    
    <view class="conversation-list">
      <view 
        class="conversation-item" 
        v-for="(item, index) in conversationList" 
        :key="index"
        @click="handleItemClick(item)"
      >
        <text class="conversation-title">{{ item.title }}</text>
        <text class="conversation-date">{{ item.date }}</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { ref } from 'vue'

// 会话列表数据
const conversationList = ref<any[]>([
  {
    id: 1,
    title: '今天看什么',
    date: '8-03'
  },
  {
    id: 2,
    title: '豆瓣前100的电影是什么',
    date: '8-03'
  },
  {
    id: 3,
    title: '什么电影适合小朋友看，小朋友可以看懂并且...',
    date: '8-03'
  },
  {
    id: 4,
    title: '宫崎骏的电影',
    date: '8-03'
  },
  {
    id: 5,
    title: '姜文的电影',
    date: '8-03'
  }
])

// 查看全部
const handleViewAll = () => {
  console.log('查看全部相关会话')
}

// 点击会话项
const handleItemClick = (item: any) => {
  console.log('点击会话:', item)
}
</script>

<style lang="scss" scoped>
.related-conversation {
  padding: 0rpx;
  background-color: #fff;
  border-radius: 16rpx;
  margin-bottom: 24rpx;
  
  .header {
    margin-top: 20rpx;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 20rpx 10rpx;
    
    .title {
      font-size: 28rpx;
      font-weight: 600;
      color: rgba(21, 23, 31, 1);
      line-height: 44rpx;
    }
    
    .view-all {
      display: flex;
      flex-direction: row;
      align-items: center;
      
      .view-all-text {
        font-size: 24rpx;
        color: rgba(130, 136, 148, 1);
        margin-right: 5rpx;
        line-height: 38rpx;
      }
    }
  }
  
  .conversation-list {
    .conversation-item {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      padding: 15rpx 10rpx;
      transition: background-color 0.2s;
      
      &:last-child {
        border-bottom: none;
      }
      
      &:active {
        background-color: rgba(12, 20, 102, 0.04);
      }
      
      .conversation-title {
        flex: 1;
        font-size: 28rpx;
        color: #15171f;
        line-height: 44rpx;
        margin-right: 24rpx;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      
      .conversation-date {
        font-size: 28rpx;
        color: #999;
        line-height: 42rpx;
        flex-shrink: 0;
      }
    }
  }
}
</style>