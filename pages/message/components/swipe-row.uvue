<template>
  <view
    class="chat-swipe"
    @touchstart="onTouchStart($event)"
    @touchmove="onTouchMove($event)"
    @touchend="onTouchEnd($event)"
    @touchcancel="onTouchEnd($event)"
  >
    <!-- 默认插槽 = 行内容（不动）；按钮层从右缘重叠滑入/收回（飞书同款） -->
    <slot />
    <view class="chat-swipe__acts" :style="actsTranslateStyle">
      <slot name="acts" />
    </view>
  </view>
</template>

<script setup lang="uts">
  import { computed, ref, watch } from "vue";

  const props = defineProps({
    // 受控收起：true=该行处于展开态。展开不由此 prop 驱动（跟手拖拽/松手吸附自治），
    // prop 变 false 时内部带动画收回——父页单开策略（关其它行）/点行收起/点按钮收起走这里。
    open: {
      type: Boolean,
      default: false,
    },
    // 按钮区总宽（rpx）：420=3×140 / 280=2×140 / 220=单个宽钮
    actsRpx: {
      type: Number,
      default: 420,
    },
  });

  const emit = defineEmits<{
    /** touchstart：父页收起其它已展开行（单开策略） */
    grab: [];
    /** 轴判定为横向：锁列表滚动（direction=none，防斜滑时页面上下晃动） */
    lock: [];
    /** 松手/取消：解锁列表滚动 */
    unlock: [];
    /** 松手吸附为展开（过半）：父页记入 openRowId */
    opened: [];
    /** 松手吸附为收回（不足半）：父页清出 openRowId */
    closed: [];
    /** 横向滑动松手：H5 会补发一次 click，父页置位吞掉避免误触行点击 */
    swiped: [];
  }>();

  /**
   * rpx→px 换算基准（windowWidth/750，组件创建时取一次缓存）：
   * 跟手滑动期间不再跨端调 uni.getWindowInfo（原生桥调用 × 每帧，是左滑跟手延迟的主因）。
   */
  let rpxScalePx = uni.getWindowInfo().windowWidth / 750;
  /** 本行按钮区宽度（px）：actsRpx 按行类型固定，算一次即可。 */
  let actsWidthPx = rpxScalePx * props.actsRpx;

  // 按钮层横向偏移（px，闭合=0，展开=-W）：内容行不动，按钮层 translateX( swipeX + W ) 滑入/收回
  const swipeX = ref<number>(0);
  // 松手吸附动画开关（200ms 过渡）
  const swipeAnim = ref<boolean>(false);

  const actsTranslateStyle = computed((): string => {
    let style = `transform: translateX(${swipeX.value + actsWidthPx}px);`;
    if (swipeAnim.value) {
      style += "transition-property: transform; transition-duration: 200ms;";
    }
    return style;
  });

  // 外部收起：open 变 false 且当前展开 → 动画收回
  watch(
    (): boolean => props.open,
    (open: boolean) => {
      if (open != true && swipeX.value != 0) {
        swipeAnim.value = true;
        swipeX.value = 0;
      }
    },
  );

  // ===== 左滑手势（单行自治；跟手的每帧状态更新只落在本组件内，不重渲整个列表页）=====
  let touching = false;
  let startX = 0;
  let startY = 0;
  let baseX = 0;
  // 轴锁定：0=未判定 1=横向（左滑）2=纵向（让位列表滚动）
  let axis = 0;

  /**
   * 模板内联表达式中的 `$event` 在 Android UTS 会被推断为 Any；
   * 仅在模板边界接收 Any，进入手势逻辑后恢复为真实的 UniTouchEvent。
   */
  function onTouchStart(eventValue: Any): void {
    const e = eventValue as UniTouchEvent;
    touching = true;
    startX = e.touches[0].clientX;
    startY = e.touches[0].clientY;
    baseX = swipeX.value;
    axis = 0;
    emit("grab");
  }

  function onTouchMove(eventValue: Any): void {
    if (touching != true) {
      return;
    }
    const e = eventValue as UniTouchEvent;
    const dx = e.touches[0].clientX - startX;
    const dy = e.touches[0].clientY - startY;
    const adx = dx < 0 ? -dx : dx;
    const ady = dy < 0 ? -dy : dy;
    // 轴锁定：位移超 8px 才判定方向，|dx|>|dy| 为横向，否则让位纵向滚动
    if (axis == 0) {
      if (adx < 8 && ady < 8) {
        return;
      }
      axis = adx > ady ? 1 : 2;
      if (axis == 1) {
        // 横向判定成立即锁列表滚动，吃掉剩余纵向分量（解锁在 onTouchEnd/cancel）
        emit("lock");
      }
    }
    if (axis != 1) {
      return;
    }
    let next = baseX + dx;
    if (next < -actsWidthPx) {
      next = -actsWidthPx;
    }
    if (next > 0) {
      next = 0;
    }
    // 取整到像素：触摸坐标是浮点，亚像素更新只会刷无效帧徒增延迟；值未变则不触发响应式更新
    const snapped = Math.round(next);
    if (snapped == swipeX.value) {
      return;
    }
    swipeAnim.value = false;
    swipeX.value = snapped;
  }

  function onTouchEnd(_eventValue: Any): void {
    if (touching != true) {
      return;
    }
    touching = false;
    if (axis != 1) {
      return;
    }
    // 解锁列表滚动（touchcancel 也走这里：滚动被原生抢占时避免锁死）
    emit("unlock");
    // 横向滑动松手后 H5 仍会补发一次 click，父页吞掉避免误触行点击
    emit("swiped");
    // 过半展开、不足半收回；松手吸附带 200ms 过渡
    swipeAnim.value = true;
    swipeX.value = swipeX.value < -actsWidthPx / 2 ? -actsWidthPx : 0;
    emit(swipeX.value == -actsWidthPx ? "opened" : "closed");
  }
</script>

<style scoped lang="scss">
  /* 左滑操作行（飞书同款）：内容行不动，按钮层绝对定位盖在行内容上方重叠滑入 */
  .chat-swipe {
    position: relative;
    overflow: hidden;
  }

  .chat-swipe__acts {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    z-index: 2;
    display: flex;
    flex-direction: row;
  }
</style>
