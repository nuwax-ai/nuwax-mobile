<template>
  <view
    class="chat-swipe"
    hover-class="chat-swipe--hover"
    @touchstart="onTouchStart($event)"
    @touchmove="onTouchMove($event)"
    @touchend="onTouchEnd($event)"
    @touchcancel="onTouchEnd($event)"
  >
    <!-- 默认插槽为行内容；右侧平铺操作按钮层整体滑入/收回。 -->
    <slot />
    <!-- 操作按钮横向平铺，按钮层整体随手势从右侧拉出。 -->
    <view class="chat-swipe__acts" :style="actsTranslateStyle">
      <slot name="acts" />
    </view>
  </view>
</template>

<script setup lang="uts">
  import { computed, ref, watch } from "vue";

  const props = defineProps({
    /**
     * 受控收起：true 时该行处于展开态。展开不由此 prop 驱动（跟手拖拽/松手吸附在子组件自治），
     * prop 变 false 时内部带动画收回——父页单开策略（关其它行）/点行收起/点按钮收起走这里。
     */
    open: {
      type: Boolean,
      default: false,
    },
    // 按钮区总宽（rpx）：420=3×140 / 280=2×140 / 220=单个宽钮
    actsRpx: {
      type: Number,
      default: 420,
    },
  });

  const emit = defineEmits<{
    /** touchstart：父页收起其它已展开行（单开策略） */
    grab: [];
    /** 轴判定为横向：锁列表滚动（direction=none，防斜滑时页面上下晃动） */
    lock: [];
    /** 松手/取消：解锁列表滚动 */
    unlock: [];
    /** 松手吸附为展开（过半）：父页记入 openRowId */
    opened: [];
    /** 松手吸附为收回（不足半）：父页清出 openRowId */
    closed: [];
    /** 横向滑动松手：H5 会补发一次 click，父页置位吞掉避免误触行点击 */
    swiped: [];
  }>();

  /**
   * rpx→px 换算基准（组件创建时取一次缓存）：
   * 跟手滑动期间不再跨端调 uni.getWindowInfo（原生桥调用 × 每帧，是左滑跟手延迟的主因）。
   */
  const rpxScalePx = uni.getWindowInfo().windowWidth / 750;
  /** 本行按钮区总宽（px）：actsRpx 按行类型固定，算一次即可。 */
  const actsWidthPx = rpxScalePx * props.actsRpx;

  // 按钮层横向偏移（px，闭合=0，展开=-totalW）
  const swipeX = ref<number>(0);
  // 松手吸附动画开关（200ms 过渡）
  const swipeAnim = ref<boolean>(false);

  const actsTranslateStyle = computed((): string => {
    let style = `transform: translateX(${swipeX.value + actsWidthPx}px);`;
    if (swipeAnim.value) {
      style += "transition-property: transform; transition-duration: 200ms; transition-timing-function: ease-out;";
    }
    return style;
  });

  // 外部收起：open 变 false 且当前展开 → 动画收回
  watch(
    (): boolean => props.open,
    (open: boolean) => {
      if (open != true && swipeX.value != 0) {
        swipeAnim.value = true;
        swipeX.value = 0;
      }
    },
  );

  // ===== 左滑手势（单行自治；跟手的每帧状态更新只落在本组件内，不重渲整个列表页）=====
  let touching = false;
  let startX = 0;
  let startY = 0;
  let baseX = 0;
  // 轴锁定：0=未判定 1=横向（左滑）2=纵向（让位列表滚动）
  let axis = 0;
  let lastMoveLogAt = 0;

  /**
   * 模板内联表达式中的 `$event` 在 Android UTS 会被推断为 Any；
   * 仅在模板边界接收 Any，进入手势逻辑后恢复为真实的 UniTouchEvent。
   */
  function onTouchStart(eventValue: Any): void {
    const e = eventValue as UniTouchEvent;
    if (e.touches.length == 0) {
      console.log("[IMSwipe] touchstart without touches");
      return;
    }
    touching = true;
    startX = e.touches[0].clientX;
    startY = e.touches[0].clientY;
    baseX = swipeX.value;
    axis = 0;
    lastMoveLogAt = 0;
    console.log(`[IMSwipe] touchstart x=${startX} y=${startY} base=${baseX}`);
    emit("grab");
  }

  function onTouchMove(eventValue: Any): void {
    if (touching != true) {
      return;
    }
    const e = eventValue as UniTouchEvent;
    const dx = e.touches[0].clientX - startX;
    const dy = e.touches[0].clientY - startY;
    const adx = dx < 0 ? -dx : dx;
    const ady = dy < 0 ? -dy : dy;
    // 轴锁定：位移超 8px 才判定方向，|dx|>|dy| 为横向，否则让位纵向滚动
    if (axis == 0) {
      if (adx < 8 && ady < 8) {
        return;
      }
      axis = adx > ady ? 1 : 2;
      console.log(`[IMSwipe] axis=${axis == 1 ? "horizontal" : "vertical"} dx=${dx} dy=${dy}`);
      if (axis == 1) {
        // 横向判定成立即锁列表滚动，吃掉剩余纵向分量（解锁在 onTouchEnd/cancel）
        emit("lock");
      }
    }
    if (axis != 1) {
      return;
    }
    let next = baseX + dx;
    if (next < -actsWidthPx) {
      next = -actsWidthPx;
    }
    if (next > 0) {
      next = 0;
    }
    // 取整到像素：触摸坐标是浮点，亚像素更新只会刷无效帧徒增延迟；值未变则不触发响应式更新
    const snapped = Math.round(next);
    if (snapped == swipeX.value) {
      return;
    }
    swipeAnim.value = false;
    swipeX.value = snapped;
    const now = Date.now();
    if (now - lastMoveLogAt >= 100) {
      lastMoveLogAt = now;
      console.log(`[IMSwipe] move dx=${dx} swipeX=${snapped}px`);
    }
  }

  function onTouchEnd(_eventValue: Any): void {
    if (touching != true) {
      console.log("[IMSwipe] touchend ignored: no active touch");
      return;
    }
    touching = false;
    console.log(`[IMSwipe] touchend axis=${axis} swipeX=${swipeX.value}px`);
    if (axis != 1) {
      console.log(`[IMSwipe] touchend ignored: axis=${axis}`);
      return;
    }
    // 过半展开、不足半收回；松手吸附带 200ms 过渡
    swipeAnim.value = true;
    swipeX.value = swipeX.value < -actsWidthPx / 2 ? -actsWidthPx : 0;
    const opened = swipeX.value == -actsWidthPx;
    console.log(`[IMSwipe] settle swipeX=${swipeX.value}px width=${actsWidthPx}px opened=${opened}`);
    // 先提交本行最终位移，再通知父级变更 scroll-view direction，避免原生视图更新打断收尾。
    emit(opened ? "opened" : "closed");
    emit("swiped");
    emit("unlock");
  }
</script>

<style lang="scss">
  /* 左滑操作行（飞书同款）：内容行不动，按钮层绝对定位盖在行内容上方重叠滑入。
     不加 scoped：slot 内容由父页 message.uvue 渲染，scoped 会把 .chat-swipe__act
     改写成 .chat-swipe__act[data-v-xxx] 让父页渲染的按钮拿不到这条样式（行内块
     自然铺开 = 全部按钮可见），与容器样式只此一处使用、无命名冲突故无需 scoped */
  .chat-swipe {
    position: relative;
    overflow: hidden;
  }

  .chat-swipe__acts {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    z-index: 2;
    display: flex;
    flex-direction: row;
  }
</style>
